# Vite reads only variables prefixed with VITE_
#
# If empty, the app will call /v2/... on the same domain.
# Recommended for single-server setup where nginx/caddy proxies /v2/ -> backend.
VITE_BACKEND_BASE_URL=
